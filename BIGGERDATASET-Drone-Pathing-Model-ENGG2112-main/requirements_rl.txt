# ── Step 1: Install PyTorch with CUDA 12 support (RTX 4080) ──────────────────
# Run this command FIRST, separately in your terminal:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# ── Step 2: Install the remaining packages ────────────────────────────────────
#
#   pip install -r requirements_rl.txt

gymnasium>=0.29.0
stable-baselines3>=2.3.0
tensorboard>=2.17.0

# Already installed (included here so pip can verify versions):
numpy>=1.24.0
matplotlib>=3.7.0
rasterio>=1.3.0
